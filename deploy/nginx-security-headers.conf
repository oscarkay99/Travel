# Loaded inside the existing Nginx http{} context by the deployment workflow.
# HSTS intentionally excludes subdomains until every subdomain has been
# independently verified as HTTPS-only.
server_tokens off;
client_max_body_size 16k;
client_body_timeout 10s;
client_header_timeout 10s;

include /etc/nginx/security-headers.inc;
